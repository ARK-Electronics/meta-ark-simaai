# eLxr LightDM/X defaults: screensaver + DPMS 10 min. No keyboard/mouse
# means the HDMI panel stays Off after idle even when already connected.
Section "ServerFlags"
	Option "BlankTime" "0"
	Option "StandbyTime" "0"
	Option "SuspendTime" "0"
	Option "OffTime" "0"
EndSection
